apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: nodejs-gateway
  namespace: default
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: nodejs-dr
  namespace: default
spec:
  host: nodejs
  subsets:
    - name: v1
      labels:
        version: v1
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: nodejs
  namespace: default
spec:
  hosts:
    - "*"
  gateways:
    - nodejs-gateway
  http:
    - route:
        - destination:
            host: nodejs
            port:
              number: 8080
